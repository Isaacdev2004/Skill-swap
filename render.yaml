# Render Blueprint — SkillSwap API
services:
  - type: web
    name: skillswap-api
    runtime: node
    region: frankfurt
    plan: free
    rootDir: backend
    buildCommand: NPM_CONFIG_PRODUCTION=false npm install --include=dev && npm run build
    startCommand: npm start
    # Do NOT use the build command as startCommand — start must be: npm start
    healthCheckPath: /api/v1/health
    envVars:
      - key: NODE_ENV
        value: production
      - key: MONGODB_URI
        sync: false
      - key: JWT_ACCESS_SECRET
        generateValue: true
      - key: JWT_REFRESH_SECRET
        generateValue: true
      - key: COOKIE_SECRET
        generateValue: true
      - key: ALLOWED_ORIGINS
        sync: false
      - key: FRONTEND_URL
        sync: false
      - key: REDIS_ENABLED
        value: "false"
      - key: USE_MEMORY_DB
        value: "false"
